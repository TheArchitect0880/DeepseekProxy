# Copy to .env and fill in your values.

# DeepSeek Bearer token (from Android app login)
DS_BEARER_TOKEN=

# Optional bearer token to protect the proxy (leave empty for no auth)
PROXY_API_KEY=

PORT=8080
